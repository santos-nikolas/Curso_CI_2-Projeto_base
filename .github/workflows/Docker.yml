name: Docker

on:
  workflow_call:
  

jobs:

  dokcer:
    runs-on: 'ubuntu-latest'
    strategy:
    steps:
    - uses: actions/checkout@v3

    - name: setup Docker Build
      uses: docker/setup-buildx-action@v3.12.0
      
    - name: Docker Login
      uses: docker/login-action@v3.6.0
      with:
        username: nikolassantos
        password: ${{ secrets.PASSWORD_DOCKER_HUB }}
